function testing(testArg){
  // console.log(testArg);
}

// testing(null);

var arayy = [0,1,2,3];

//Opera Errors "arayy.foreach is not a function"
// arayy.forEach(function(index, something, yoyo){
  // console.log(index, something, yoyo);
// })
